const connection = require("../config/database");
// async function getAllCourses(){
//     const [results, field] = await connection.execute("select * from course");
    
//     return results;
// }


module.exports = {
    getAllCourses
}